/**
    \brief Holds "commandlets" -- commands, optimized to run with greatest speed possible (usually via grouping) - used mostly for the graphical modules
    \todo to be done
    \todo Do we even need this?
*/

module siege.core.commandlet;

class Commandlet
{
}
