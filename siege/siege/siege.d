module siege.siege;

private
{
    import siege.all;
}

public
{
    import siege.ai.all;
    import siege.audio.all;
    import siege.core.all;
    import siege.graphics.all;
    import siege.input.all;
    import siege.modules.modules; // not all
    import siege.physics.all;
    import siege.util.all;
}
