UI
sockets
files(later)
hashes (md*, sha*, crc32...)

much, much (much!) more...

-graphics & audio modules
    -> return formats accepted (to simplify the modules)
    -> task of converting formats falls to the frontend
-clean up this folder, make a CodeBlocks workspace
