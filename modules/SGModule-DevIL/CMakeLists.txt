PROJECT(SGModule-DevIL)

FIND_PACKAGE(DevIL REQUIRED)

INCLUDE_DIRECTORIES(../include ${DEVIL_INCLUDE_DIR})
ADD_LIBRARY(SGModule-DevIL MODULE main load)
TARGET_LINK_LIBRARIES(SGModule-DevIL ${DEVIL_LIBRARY})
SET_TARGET_PROPERTIES(SGModule-DevIL PROPERTIES ENABLE_EXPORTS ON)
