PROJECT(SGModule-SDL)

INCLUDE_DIRECTORIES(../include)
ADD_LIBRARY(SGModule-SDL MODULE main joystick keyboard mouse window)
TARGET_LINK_LIBRARIES(SGModule-SDL SDL)
SET_TARGET_PROPERTIES(SGModule-SDL PROPERTIES ENABLE_EXPORTS ON)
