PROJECT(SGModule-SndFile)

INCLUDE_DIRECTORIES(../include)
ADD_LIBRARY(SGModule-SndFile MODULE main load)
TARGET_LINK_LIBRARIES(SGModule-SndFile sndfile)
SET_TARGET_PROPERTIES(SGModule-SndFile PROPERTIES ENABLE_EXPORTS ON)
