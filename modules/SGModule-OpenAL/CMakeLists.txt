PROJECT(SGModule-OpenAL)

FIND_PACKAGE(OpenAL REQUIRED)

INCLUDE_DIRECTORIES(../include ${OPENAL_INCLUDE_DIR})
ADD_LIBRARY(SGModule-OpenAL MODULE main sample source)
TARGET_LINK_LIBRARIES(SGModule-OpenAL ${OPENAL_LIBRARY})
SET_TARGET_PROPERTIES(SGModule-OpenAL PROPERTIES ENABLE_EXPORTS ON)
